<section>
        {{ form_start(form) }}
        {{ form_row(form.name) }}
        {{ form_row(form.surname) }}
                <button type="submit" class="btn btn-default">Zapisz</button>
        {{ form_end(form) }}
</section>